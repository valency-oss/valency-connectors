interface:
  display_name: "Research Landscape"
  short_description: "Map topics, trends, authors, and corpus context"
  icon_small: "./assets/favicon_solid_cyan_valency.svg"
  icon_large: "./assets/favicon_solid_cyan_valency.svg"
  brand_color: "#00E6FF"
  default_prompt: "Map this research landscape, including its trends, leading authors, and representative work."
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "valency"
      description: "Use the complete Valency Bond MCP surface"
      transport: "streamable_http"
      url: "https://mcp.valency.io/mcp/"
