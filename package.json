{
  "name": "valency-bond",
  "private": true,
  "type": "module",
  "description": "Provider plugin distributions for the Valency Bond MCP server.",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "validate:claude": "claude plugin validate . --strict && claude plugin validate plugins/claude/valency --strict",
    "validate:openai": "uv run --no-project --with PyYAML==6.0.3 python \"${CODEX_HOME:-$HOME/.codex}/skills/.system/plugin-creator/scripts/validate_plugin.py\" plugins/openai/valency"
  }
}
